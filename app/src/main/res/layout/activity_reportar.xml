<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:background="#A8E6CF"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:orientation="vertical"
        android:padding="16dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Título -->
        <TextView
            android:text="Denuncia Ciudadana"
            android:textSize="24sp"
            android:textStyle="bold"
            android:gravity="center"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp" />

        <!-- Botonera -->
        <GridLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:columnCount="3"
            android:rowCount="2"
            android:padding="8dp">

            <Button android:text="Inicio" style="@style/MenuButton" />
            <Button android:text="Reportar" style="@style/MenuButton" />
            <Button android:text="Chat" style="@style/MenuButton" />
            <Button android:text="Mis Reportes" style="@style/MenuButton" />
            <Button android:text="Perfil" style="@style/MenuButton" />
            <Button android:text="Configuración" style="@style/MenuButton" />
        </GridLayout>

        <!-- Descripción del problema -->
        <TextView android:text="Descripción del problema:" />
        <EditText
            android:id="@+id/etDescripcion"
            android:hint="Describe el problema..."
            android:minHeight="100dp"
            android:gravity="top"
            android:background="@android:drawable/edit_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp" />

        <!-- Botón para imagen/video -->
        <Button
            android:id="@+id/btnSeleccionarImagen"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Tomar foto o seleccionar imagen/video"
            android:backgroundTint="#7CA4D4"
            android:textColor="#FFFFFF"
            android:layout_marginBottom="16dp" />

        <!-- Ubicación -->
        <TextView android:text="Ubicación detectada (solo lectura):" />
        <EditText
            android:id="@+id/etUbicacion"
            android:enabled="false"
            android:text="No se pudo obtener ubicación"
            android:background="#E0E0E0"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp" />

        <!-- Enviar Reporte -->
        <Button
            android:id="@+id/btnEnviarReporte"
            android:text="Enviar Reporte"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:backgroundTint="#7CA4D4"
            android:textColor="#FFFFFF" />
    </LinearLayout>
</ScrollView>